# Requirements for classification models (Colab-friendly)
# Avoid installing Colab core packages (torch, numpy, pandas, etc.).

# imbalanced-learn removed (SMOTE no longer used)
scikit-multilearn==0.2.0
PyGithub>=2.2.0
sentence-transformers>=2.2.2
deep-translator>=1.11.4


# Hugging Face stack (safe for Colab)
# transformers>=4.46 - ensures seed parameter support in generate() method
transformers>=4.46
accelerate>=0.30
datasets>=2.19
sentencepiece>=0.1.99

# Optional (local-only) LLM providers for augmentation — comment out on Colab
# openai  # uncomment locally to enable LLM augmentation
# anthropic  # alternative provider for Claude models

